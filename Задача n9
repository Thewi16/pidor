#include <iostream>
#include <cmath>
int main() {
    double s = 0, dx = 0.001;
    for (double x = -12; x <= 12; x += dx)
        s += (12 - std::abs(x)) * dx;
    std::cout << "Площадь = " << s << "\n";
}
